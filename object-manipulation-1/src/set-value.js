/* exported setValue */
// utilize bracket notation to set the value of an object's property
// return the object from the function

function setValue(object, key, value) {
  object[key] = value;
  return object;
}
